apiVersion: v1
kind: Service
metadata:
  name: project1-env-frontend-service
  labels:
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: frontend 
    app.kubernetes.io/instance: project1-frontend
  ports:
    - name: frontend
      port: 8080
      targetPort: 8080